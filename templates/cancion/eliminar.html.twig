{% extends 'layout.html.twig' %}

{% block content %}
    <h1>Eliminar</h1>
    <div class="alert alert-danger">
        ¿eliminar la canción {{ cancion.titulo }}?
    </div>
    <form method="POST">
        <a href="{{ path('cancion_listar') }}" class="btn btn-info">Volver</a>
        <button type="submit" name="eliminar" class="btn btn-danger">borrar</button>
    </form>
{% endblock %}